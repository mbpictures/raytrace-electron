{
    "compilerOptions": {
        "outDir": "./dist/",
        "sourceMap": true,
        "noImplicitAny": true,
        "module": "es6",
        "target": "es6",
        "lib": ["webworker", "es6", "scripthost", "DOM"],
        "jsx": "react"
    },
    "exclude": [
        "node_modules" // don't run on any code in the node_modules directory
    ]
}